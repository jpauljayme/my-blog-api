version:  '3.2'

services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: johnpauljayme
      POSTGRES_PASSWORD: password
      POSTGRES_DB: blog
